//  // Get the current URL
//  var currentUrl = window.location.href;

//  // Get all links inside the navigation
//  var links = document.querySelectorAll('.nav-large-links .large-link');

//  // Loop through each link
//  links.forEach(function(link) {
//      // Check if the link's href attribute matches the current URL
//      if (link.getAttribute('href') === currentUrl) {
//          // If it matches, add the "current-page" class to the link
//          link.classList.add('current-page');
//      }
//  });

//  console.log(currentUrl);