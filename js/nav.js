// const menu = document.querySelector('menu'); // Assuming menu is a class
// const button = document.querySelector('open-menu'); // Assuming open-menu is a class

// const defaults = { duration: 1, ease: Back.easeOut.config(2) };

// var tl = gsap.timeline(defaults);

// button.addEventListener('click', () => {
//   console.log('yay');
// });
